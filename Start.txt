三个命令，分别在不同终端运行：

**1. MySQL**（通常已开机自启，无需手动启动）
```
net start MySQL83
```

**2. 后端**（FastAPI）
```
cd c:\Users\zjy\Desktop\校招\FoodTime\backend
python -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

**3. 前端**（Vite）
```
cd c:\Users\zjy\Desktop\校招\FoodTime\frontend
npm run dev
```

启动后访问 http://localhost:5173/ 即可。